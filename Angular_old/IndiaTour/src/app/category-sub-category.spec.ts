import { CategorySubCategory } from './category-sub-category';

describe('CategorySubCategory', () => {
  it('should create an instance', () => {
    expect(new CategorySubCategory()).toBeTruthy();
  });
});
